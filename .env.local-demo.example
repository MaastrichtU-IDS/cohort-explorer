# Non-secret defaults for the fully local synthetic-data demo.
# `make demo-up` writes fresh session and inter-service secrets to
# .demo-state/<namespace>/runtime.env with mode 0600.

LOCAL_AUTH_EMAIL=nikolas.molyndris@decentriq.ch
AADCRV2_REPO_DIR=../delta-aadcrv2
DEMO_PACK_HOST_DIR=./data/synthetic-demo-pack
COMPOSE_PROJECT_NAME=cohort-explorer-aadcr-demo
